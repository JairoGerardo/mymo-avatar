export default defineNuxtPlugin(() => {
  // Avatar is client-only — this plugin runs only in the browser.
  // Import the SDK here if you need a global instance, or use the
  // AvatarWidget component directly in your pages.
})
