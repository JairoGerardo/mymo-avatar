{
  "name": "@mymo/example-electron",
  "private": true,
  "version": "0.0.0",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "vite build",
    "start": "vite build && cross-env NODE_ENV=production electron ."
  },
  "dependencies": {
    "@mymosdk/avatar": "workspace:*",
    "electron": "^31.0.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "cross-env": "^7.0.0",
    "vite": "^5.4.0",
    "wait-on": "^8.0.0"
  }
}
